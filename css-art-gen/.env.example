# CSS Art Generator - Environment Variables

# Note: For MVP testing on Vercel, no environment variables are required
# The counter system will work in-memory (resets on each deployment)

# For production, you would add:
# DATABASE_URL=your_postgres_url
# IPFS_API_KEY=your_pinata_key
